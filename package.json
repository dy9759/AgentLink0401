{
  "name": "agentmesh-client",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "AgentMesh Client — MCP server + Skill for connecting to AgentMesh Hub",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
